import 'package:do_it/model/notes_model.dart';
import 'package:hive/hive.dart';

// int pendingTask(NotesModel notesModel) {
//   final box = Hive.box('myBox');
//   return box.values.where((NotesModel) => notesModel.isCompleted).length;
// }
//
// int pendingg(List list, String field) {
//   return list.where((element) => element.field == true).length;
// }
